---
tags: [session-log]
updated: 2026-09-10
status: draft
---

# 会话 2026-09-10 — 修 Co​dex 安装崩溃 + 补收尾自动化（并暴露"自动化形同虚设"）

## 一、起因：Co​dex 装不上

用户在其他机器 `abs install` 到 co​dex 时报 `cfg.hooks.filter is not a function`。

**根因**：`src/install.js` 把 Co​dex hooks 写成扁平数组 `{hooks: [{event, command}]}`，
而 Co​dex 真实 `~/.co​dex/hooks.json` 是对象形态 `{hooks: {EventName: [{matcher?, hooks:[{type,command}]}]}}`
（与 Cl​aude `settings.json` 同形）。目标机该文件已是对象 → `.filter` 崩。

**修复**（`src/install.js`）：
- 安装改对象形态，按事件合并、保留既有 hook（moshi 等）、幂等重装；
- 历史扁平数组形态**迁移**成对象，不丢用户 hook；
- **卸载侧原来只处理扁平数组** → 对象形态的 hook 永远删不掉，一并修（两形态都处理，空事件整删）。

测试：新增 3 例（对象形态幂等重装+共存+卸载只删 abs、扁平数组迁移）。发布 0.1.1。

## 二、真正的反馈：abs 的自动化形同虚设

用户指出：**"你刚刚做完的这个工作还是没有自动化到 abs，abs 的初衷就是无人值守自动总结自动 todo"**。

查证（铁证）：
- `.brain/todo.md` 停在 **2026-09-09 04:44**，`.brain/log.md` 停在 **2026-09-09 04:54**；
- 本会话 2026-09-10 10:00–10:53 干了大量实事（修 bug/发布），**`.brain` 一字未写**；
- `~/.abs/log/hooks.log` 本会话只有 `session_start`×5 + `session_shutdown`×1，**零条收尾**。

**根因（两层）**：
1. **事件缺**：pi 扩展只挂 `session_start`/`session_shutdown`。`session_shutdown` 只在**退出 pi** 时触发，
   正常干活到一半永远等不到收尾信号（Cl​aude/Co​dex 侧有 `Stop` 事件，pi 侧没有对应物）。
2. **机制被动**：`abs wrapup` 只往 `wrapup.log` 写一行快照。它"记了有滞留"，但**没有任何机制让 agent 真的去收尾**
   —— 下会话靠 `abs load` 顶出滞留，前提是"你还记得跑 abs load"。收尾仍靠自觉，而自觉不可靠。

**修复（真自动化 = hook 主动推，不靠自觉）**（`src/install.js` 的 `piPluginSource`）：
- pi 扩展挂 **`agent_end`**，满足条件时 `pi.sendUserMessage(收尾指令, {deliverAs: 'followUp'})`
  **把 agent 推去走收尾循环**（不是只写日志）。
- **触发条件收窄**（否则每轮都吵）：本会话真改过文件（`write`/`edit`/非只读 `bash`，
  只读 bash 用 `READONLY_CMD` 排除 `ls/cat/grep/git status...`）+ `.brain` 存在 + `log.md` 今日无记录；
- **每会话最多一次**（`teardownNudged` 标志）；已收尾/无图谱则不打扰。

**踩坑**：注入文本写在 JS 模板字符串里，`\n` 和 `\"` 被外层模板先吃掉，生成出跨行裸字符串
（语法坏）。须双写 `\\n` / `\\\"`。验证方式：从生成的 `abs.ts` 抠出字面量 `eval` 看最终渲染。

测试：新增 1 例断言挂 `agent_end`/用 `sendUserMessage`/有节流与判定函数。发布 0.2.0。

## 三、顺手补的 lint 盲区

收尾时 `abs lint` 报 `SOURCES-PILED-UP`（13 个 source）。归档 7 个已被 concepts 吸收的旧 source 后，
`lint` 仍报 0 problem —— 但 `index.md` 里**还列着那 7 个已删页**。

**盲区**：lint 只查 page→index（`INDEX-MISSING`），**不查 index→page**，删页后 index 的残留死引用静默留存。

**修复**（`src/store.js` `cmdLint`）：新增 `INDEX-DEAD-LINK` 反向检查。测试新增 1 例。发布 0.2.1。

## 四、本次真正走完的收尾（自我验证）

- `abs task start/done`：CO​DEX-HOOKS-FIX、TEARDOWN-AUTO 两项归位 Done；
- `abs note`：2 条经验落 sources（Co​dex 形态坑、收尾自动化缺口）；
- `abs log`：2 行工作成果；
- `abs lint`：0 problem（含归档 7 个 source + 清 index 死引用 + 修 4 处 concepts 死链）；
- 版本：0.1.0 → 0.1.1 → 0.2.0 → 0.2.1 均已发布。

## 🪝 Next Session Hook

**开场必读**：
1. **收尾注入已全线贯通（真机已验证）**：pi 真实会话跑通 `agent_end` → `seen` → `teardown-nudge`（见本文件第九节）。
   后续若怀疑失效，查 `~/.abs/log/hooks.log` 的 seen 痕（已带 `cwd=` 与 `brain=`）：
   - 无 `seen` → 事件没触发；有 `seen` 无 `nudge` → 被守卫拦下（正常）；有 `nudge` → 真注入。
   - 若 nudge 与“log.md 已有今天”矛盾 → 看 seen 行的 `cwd`/`brain` 归因。
2. **op​encode 侧仍待真机验证**：行为级已过（含 Bun 下），但**未在真 op​encode 会话**里观察过。
3. **验证插件三层缺一不可**：文件在 → `default` 能 import 出 `server` → 日志有落痕。
   字符串断言不够（曾放过 `({name})` 签名错与 `export const` 命名导出错）。
   改插件后跑 `test/plugin-behavior.test.js`。
4. **代理坑**：`~/.npmrc` 的 `192.168.0.114:7890` 常不可达，`npm publish/view` 会 EHOSTUNREACH。
   发布：临时注释 proxy 两行 → publish → **立刻恢复**（备份 `~/.npmrc.abs-bak`）。
5. 本项目无 git remote，发布只走 npm。
6. **零宽字符坑**（本仓库高发）：`op​encode`/`Co​dex` 等词在源码里带 ZWSP（U+200B）。
   混入**标识符/路径/命令行参数**会语法错或让 CLI 认不出 agent 名；注释里的无害。
   已有 ZWSP 守卫测试。改这类文件用脚本按码点核验（`String.fromCharCode(0x200b)`），别靠肉眼看。
7. **不要在自动发现目录里留 `.bak`**：`~/.pi/agent/extensions/`、`~/.config/op​encode/plugins/`
   下的备份可能被宿主宽匹配加载，造成重复 handler。备份到目录外。

## 五、op​encode 侧：插件从未触发过（第二半）

用户说"继续"后接着补 op​encode。查证发现**比预想严重**：

**op​encode 插件从来没触发过** —— `grep -c "opencode:" ~/.abs/log/hooks.log` = **0**。

**根因**：模板写成 `event: async ({ name }) => {...}`，但 `@op​encode-ai/plugin` 的真实类型是
`event?: (input: { event: Event }) => Promise<void>`，事件名在 **`event.type`**。
→ `name` 恒 `undefined` → `includes()` 恒 false → **静默失效，无任何报错**。
（事件名也错：真实是 `session.created`/`session.idle`/`session.deleted`，不是 `session.start`/`session.end`。）

**修复**（据官方 `.d.ts` 实核，非凭印象）：
- 签名改 `event: async ({ event })`，从 `event.type` 取事件名；
- 补 **收尾注入**（与 pi 同策略）：`session.idle`（= 每轮结束）时经
  `client.session.promptAsync({ path:{id:sessionID}, query:{directory}, body:{parts:[{type:'text',text}]} })` 注入收尾指令；
- 真改过文件由 **`tool.execute.after`** 观测（`write`/`edit`/`patch` 类工具名）；
- 同四条件收窄 + 每会话一次节流。

**验证**：
- 生成插件语法 OK；行为级模拟测试 **8/8**（只读不注入/写后注入一次/再 idle 节流/今日已收尾不注入/无图谱不注入/日志落痕）；
- 与官方 `Plugin` 契约校验通过（`(input) => Promise<Hooks>`，返回 `{event, 'tool.execute.after'}`）；
- 已装到真实 `~/.config/op​encode/plugins/abs.ts`（旧版备份）。

新增 2 条回归测试（断言挂对事件、用对签名、不得用 `({name})`）。发布 **0.3.0**。

**踩坑**：插入代码时我自己用 `Z='\u200b'` 拼接，把 ZWSP 写进了函数**标识符**
（`function op<ZWSP>encodePluginSource`）→ 直接 SyntaxError；测试里也把 ZWSP 混进
`'--agent','op<ZWSP>encode'` 与路径参数 → CLI 认不出 agent。教训：改这类文件用脚本按码点核验。

## 六、重启后真机校验（用户 /q 要求）

用户重启后要求“检查一下机制”。实测结果：

### pi —— ✅ 已加载并运行
- `~/.abs/log/hooks.log` 出现 `[11:25:43] pi:session_start` → 新扩展确实加载了；
- 但 `agent_end:teardown-nudge` = **0** —— **这是设计正确**，不是失败：
  `.brain/log.md` 已有今日日期（我早些写了 log 行）→ `loggedToday` 守卫正确地抑制了打扰。
- 为真验证注入路径，直接 import **真实部署的** `~/.pi/agent/extensions/abs.ts`
  （Node 24 原生剥类型）跑行为测试：**7/7 通过**
  （只读不注入 / bash 只读不注入 / 写后注入一次且 deliverAs=followUp / 再触发节流 /
  今日已收尾不注入 / 无图谱不注入 / 日志落痕）。

### op​encode —— ❌ 又挖出第二个静默失效
- `op​encode:` 日志仍为 **0**。上轮已修签名与事件名，为何还不触发？
- **根因（独立于上轮）**：插件用 `export const AbsPlugin`（**命名导出**），
  而 op​encode 加载器取 **`default`** → `mod.default === undefined` → **插件根本没被加载**。
  官方类型 `PluginModule = { id?: string; server: Plugin; tui?: never }` 已明示 `server` 字段；
  同机可用的 `moshi-hooks.ts` 也是 `export default { id, server }`。
- **修复**：`const server = async ({client,directory}) => ({...})` + `export default { id: "abs", server }`。
- **验证**：`import('/tmp/oc2.ts')` → 导出键 = `default`，`default.id = 'abs'`，`server` 是函数；
  行为测试 **8/8** 通过；已部署到真实 `~/.config/op​encode/plugins/abs.ts`（验证 `default.id/server` 就绪）。

### 结论：“装上了”三层要分开证
文件在 ≠ 加载了 ≠ 触发了。上轮只证到第二层（契约/语法），放过了导出方式错误。
新增回归测试：断言 `export default { id, server }` 且**不得**用命名导出。发布 **0.3.1**。

### 仍未验证的一层
两者都只在**单元/行为模拟**里证过，**真实宿主事件驱动**仍未跑过
（需下个真会话的 `agent_end` / `session.idle` 落痕）。

## 七、行为级测试 + ZWSP 守卫（第三半）

继续把“最后一层”补上：

### 7.1 真机端到端贯通（双宿主）
用**真实部署的产物**（非模板）+ 真实文件系统 + 真实事件序列：
- pi: import `~/.pi/agent/extensions/abs.ts` → 驱动 `agent_end` → 注入 1 条、
  `deliverAs=followUp`、正文渲染正确、`pi:agent_end:teardown-nudge` 落痕 ✅
- op​encode: import `~/.config/op​encode/plugins/abs.ts` → `default.server` → 驱动
  `session.created`+`tool.execute.after`+`session.idle` → 注入 1 条、落痕正确 ✅
- 另在 **Bun**（op​encode 真实运行时）下复验加载与注入 ✅

### 7.2 为何之前会漏：字符串断言不够
`install.test.js` 只做**源码字符串断言**，于是两个真 bug **两次全绿**：
① `({name})` 签名错；② `export const` 命名导出被加载器忽略。
→ 新建 `test/plugin-behavior.test.js`：真 import 生成产物 + 驱动事件 + 断言日志落痕。
pi 5 例 / op​encode 4 例，共 9 例。

### 7.3 ZWSP 守卫（新测试自己拓出来的真 bug）
新加的 ZWSP 守卫测试**当场拓出**一个真问题：插件写日志时前缀是 `op​encode:`
（带 U+200B）—— 纯负担，grep/断言都得跟着带 ZWSP（本次就被坑了多次）。
→ 日志前缀改纯 ASCII，并加守卫测试：
- 断言 CLI 识别**不带 ZWSP** 的 agent 名（op​encode/pi/co​dex/cl​aude-code）；
- 断言生成物**代码行**（非注释）不含 ZWSP。

共 144 测试绿（+11）。发布 **0.4.0**。

## 八、可观测性：补 seen 痕（第四半）

排查中发现一个**无判断依据**的问题：插件只在**真注入**（nudge）时写日志，
所以看 `hooks.log` 时**无法区分三种状态**：
① 事件根本没触发（扩展没加载）② 触发了但被守卫拦下（今日已收尾/无图谱/只读）③ 真注入了。

本会话（11:25 重启后）跑了大量 turn、大量 write/edit/bash，
`agent_end` 却**零痕** —— 分不清是“扩展没跑”还是“跑了被拦下”（实际是后者，
因为 `log.md` 已有今日记录）。

**修法**：首次 `agent_end` / `session.idle` **无条件**写一行 `seen` 痕（只记一次，不刷屏）。
→ 三态变为：**无痕** = 未触发；**有 seen 无 nudge** = 被拦下；**有 nudge** = 真注入。

**通则**：判断“机制是否生效”的日志，必须有一条**不受任何守卫影响**的痕。

新增 2 条测试（断言被拦下时仍有 seen 痕、且只记一次）。共 **146 测试绿**。发布 **0.5.0**。

## 九、真机验证通过（第五半）

`~/.abs/log/hooks.log` 出现 **真实 pi 会话**的完整链路：
```
[11:42:54] pi:session_start
[11:43:23] pi:agent_end:seen           ← 事件触发了
[11:43:23] pi:agent_end:teardown-nudge ← 真注入了
```
**不是测试造的**：我的测试写 `/tmp/seen/log`（11:43:38 一条），而这两条在**真实日志**里、
时间也不同（11:43:23）—— 铁证真实会话确实跑通了全链路。

### 顺手把“异常不可归因”补上
发现一条 nudge 与“`log.md` 已有今天”矛盾，但**无法事后解释**（没记 cwd/brain）。
→ seen 痕补记 `cwd=` 与 `brain=`，下次一眼就能归因。
这是“可观测性”原则的再一次应用：**痕必须带足够的上下文才能事后解释异常**。

### 另清理了一处隐患
我早先手动创建的 `.bak` 备份（`~/.pi/agent/extensions/abs.ts.bak-*`、
`~/.config/op​encode/plugins/abs.ts.bak-*`）落在**自动发现目录**里 ——
虽然 `*.ts` 通配不匹 `.ts.bak-*`，但仍是风险（若宿主宽匹配就会加载旧版并注册竞争 handler）。已删除。

发布 **0.5.1**。

## 十、发布正式版 1.0.0（收尾）

### 发布前真机冒烟（从 npm 包，不是开发目录）
`npm pack` → 装到干净沙箱 → 逐项验证：
- **四宿主安装**全通过（cl​aude-code / co​dex / op​encode / pi）
- **CLI 全链路**：init → task start/done → note → log → load → query → lint(0 problem)
- **MCP**：`tools/list` 返回 **9 个工具**（resolve_project/board/load/task/status/query/lint/note/wrapup）
- **产物可加载**：pi 扩展 `default` 是函数；op​encode 插件 `default.server` 是函数
- **配置形态正确**：co​dex `hooks.json` 是对象形态（3 事件）；cl​aude `settings.json` 4 事件

### 发布与全局升级
- `0.5.1` → **`1.0.0`**（npm latest，共 10 个版本）
- 全局包从 `0.1.0` 升到 `1.0.0`（hook 脚本烧的是全局路径，必须同步）
- 重跑 `abs install` 刷新四宿主 hook / skill 到 1.0.0

### 收尾对账
- todo 看板：Today 无滞留（9 项今日任务均归位 Done）
- index 路线从「D0-D4」更新为「v1.0.0 已发布 + 下一阶段候选」
- lint 0 problem

### 版本演进（本日）
`0.1.0`(初版) → `0.1.1`(Co​dex 崩溃) → `0.2.0`(pi 收尾注入) → `0.2.1`(lint 反向检查)
→ `0.3.0`(op​encode 修复+注入) → `0.3.1`(default 导出) → `0.4.0`(行为级测试)
→ `0.5.0`(seen 痕) → `0.5.1`(seen 带 cwd) → **`1.0.0`**

## 十一、补 abs update / --version（真收尾）

用户报："在另外一台电脑上 update，没发现新版本"。

**根因（不是缓存、不是网络）**：**`abs update` 命令根本不存在**。
跑它会输出 `未知命令: update`。同理 `abs --version` / `-v` 也不存在 ——
用户既无法查版本，也无法通过 abs 升级，只能自己知道去跑 `npm i -g`。

这是真缺口：用户的心智模型是"abs 自我管理"（install/uninstall 确实在），
但升级路径断了。

**修复**：
- `abs --version` / `-v` / `version` → 读包 `package.json` 输出版本；
- `abs update` → `npm view` 查最新 → 对比当前 → 有新版则 `npm i -g @latest`
  → **自动重跑 install 刷新四宿主 hook/skill**（hook 脚本烧的是绝对路径 + 模板快照，
  升级后不刷新就用不上新代码）；网络/代理失败时给明确的手动命令。
- help 列出两者（自我管理命令必须可发现）。

新增 2 条测试（三种 --version 别名输出包版本；help 含 update/--version）。共 **148 测试绿**。
发布 **1.1.0**，已从线上 tarball 验证命令存在。

### 教训
“命令看不到新版本”先分两类：**实现不存在** vs **数据/网络不对**。
本次是前者 —— 我第一反应去查代理/缓存，差点搞错方向。先跑一遍命令看输出。

## 关联连接

- [[abs-install-layout]] — 共存节 + Co​dex hooks.json 形态坑
- [[npm-publish-flow]] — 发布流程与代理坑
- [[AgentBrainSync]] — 项目实体页

## 📦 任务归档

### 2026-09-10


- [x] LOAD-RECENT-ORDER — abs load 的「最近动作」贴的是最旧 5 条而不是最新：log.md 是新在上（头=今天23:18，尾=2026-09-08），而 cmdLoad 用 tailLines(log,5)=lines.slice(-5) 取尾部=最旧。实测 load 输出里最近动作全是 09-08 的旧条目，今天的最新记录从未显示。修法: 取前 N 条（或抽前 5 个 '## [' 条目），标签改「最新 5 条」。影响: abs load 是开机读状态入口，这一节目前无用。验证: 断言输出含今天最新条目、不含 09-08 最旧条目  【落地】 (完成 2026-09-10)
- [x] ROADMAP-STALE — index.md 的路线图停在「v1.2.2 已发布，166 测试绿」，实际已到 1.5.1 / 189 测试（滞后 3 个版本）。这已在路线图自己的「下一阶段候选」里写着（"index 路线图随版本更新"）却没人做——无机制则必漂移。可选修法: ①发版时在 abs update/release 流程里同步写路线图版本 ②直接删掉路线图里的版本号（只留方向，避免这类失真） ③加 lint 检查 README/index 里的版本号与 package.json 一致  【落地】 (完成 2026-09-10)
- [x] OVERSIZE-THRESHOLD-8K — OVER-SIZE 阈值放宽 5120B → 8192B（行数 150 不变），并提成常量 PAGE_MAX_BYTES/PAGE_MAX_LINES（原来检查条件与提示文本各写一份 150/5120，会漂移）。依据：跨 4 项目 68 页仅 2 页超限且都只超一点（codebuddy 5463B / faxuehui 5440B）；早先为满足它还把一页从 5319B 压到 4972B。放宽后两者归零。已加边界测试（6KB 放行 / 9KB 报）+ revert-check  【落地】 (完成 2026-09-10)
- [x] LINT-PATH-PREFIX — lint 提示里的路径省略了 .brain/ 前缀，用户按报的路径去项目里找找不到（codebuddy 的 concepts/fnos-native-release-deploy.md 实际在 .brain/concepts/ 下，项目根没有 concepts/ 目录）。根因: listPages 里 rel=`${d}/${f}` 是 vault 相对；7 处提示(NO-FRONTMATTER/TEMPLATE-LINK/DEAD-LINK/ORPHAN-PAGE/UNRESOLVED-CONFLICT/OVER-SIZE/INDEX-MISSING)全用它。改法: rel 改 `.brain/${d}/${f}`。影响面: 只 lint 文本，无程序消费；但 test/store.test.js 两处正则（ORPHAN-PAGE/INDEX-MISSING 带 sessions\/ 的）需同步  【落地】 (完成 2026-09-10)
- [x] LINT-TOPLEVEL-PAGE — lint 误报 [[todo]] 死链：names 集合只含子目录页(concepts/entities/sources/syntheses/sessions)，.brain 顶层文件 index.md/log.md/todo.md 不被当作可链接页 → [[todo]] 被判 DEAD-LINK + INDEX-DEAD-LINK。实测 codebuddy 报 5 条、faxuehui 报 7 条，均属误报（todo.md 真实存在）。已修：names 补充 .brain 顶层 *.md 的 slug（仅用于链接目标存在性判定，不影响 ORPHAN/INDEX-MISSING）。验证：codebuddy 5 条消失、[[log]]/[[index]] 同样放行、真不存在的页仍报；新增回归测试，187 测试绿。待办：commit + 发版  【落地】 (完成 2026-09-10)
- [x] SKILL-TRIAGE-PROTOCOL — 在 skill 加「新需求受理协议」：用户报 bug 或要求加功能时，agent 不得直接动手 —— ①先总结方案(问题复述/根因或做法/改哪些文件/怎么验证) ②abs todo add 登记 ③明确问用户是否开工，等确认再改代码。例外：用户已说"开工/直接做"、一行级修字/纯查询/纯沉淀、同一需求已登记且确认过。落点: skill/SKILL.md 新增节（紧随「触发总入口」）；需重装四宿主 skill 才生效  【落地】 (完成 2026-09-10)
- [x] TODO-ARCHIVE-CMD — Done 区归档做成真流程: ①只保留近3天(可配) ②任一天内有未完成(- [ ])则整天不归档 ③归档为单个文件+在 todo 的 ### 归档 段留 `<slug>` 完成任务 N 条。含定时/触发规划 + lint 兜底  【落地】 (完成 2026-09-10)
- [x] MCP-PATH-UNSTABLE — 四宿主 abs MCP 注册路径不一致: codex 指向仓库(/Users/fanchao/Code/skills/.../bin/mcp.js), CC/opencode/pi 指向全局(lib/node_modules/...)。根因两处: ①install.js 三处都用 join(ABS_DIR,'bin','mcp.js'), 而 ABS_DIR 取决于'install.js 自己住哪' —— 从仓库跑 abs install 就写仓库路径 ②codex 分支 if(!text.includes('[mcp_servers.abs]')) 才写, 已存在即跳过 → 一旦写错永不修正。危害: 改了仓库但没装全局时, 真机跑的可能是另一份; 卸载全局后路径直接失效。修法: 路径应固定为该包的稳定安装位置(优先全局), 且已存在时也应更新为当前正确路径  【落地】 (完成 2026-09-10)
  ↳ 断点: 已修: install.js 新增 mcpEntryPath() 优先解析全局 node_modules 位置, 取不到才回退 ABS_DIR; codex 分支由"已存在即跳过"改为"校对并校正路径"。真实配置已校正(四宿主现一致指向全局)。新增 2 测试 + revert-check(首版测试太弱抓不到, 改为断言"存在全局包时必须写全局路径"后通过)
- [x] MCP-LIVE-VERIFY — 重启后 MCP 写通道 + mcp.log 双验证  【落地】 (完成 2026-09-10)
- [x] MCP-TRACE-DEAD — withTrace 定义了但 9 处工具都没包它 → mcp.log 从不生成(可观测性静默缺失,无任何症状)。已修: 统一经 tool() 注册,内部总是包 withTrace,遗漏不再可能; 新增 2 测试 + revert-check  【落地】 (完成 2026-09-10)
- [x] MCP-SMOKE-TEST — 验证 MCP 写通道（走 abs_abs_task 的 add action）  【落地】 (完成 2026-09-10)
- [x] FIX-SILENT-NOOP — 已修: abs todo 统一读写(add/start/note/blocked/done), 旧 task/board 报错提示, 只读命令拒绝多余参数, wrapup/teardown-check 移出 help。同步改 skill/README/MCP + 9 处代码内旧命令名  【落地】 (完成 2026-09-10)
- [x] DAEMON-IPC — 方案A: CLI/MCP 共用常驻 daemon (Unix socket), 消除 22ms node 启动开销 + 统一锁. 要点: ①daemon 自拉起+心跳 ②CLI 连不上时退化本地直读(安全网,必须可测) ③ABS_NO_DAEMON=1 开关 ④MCP server 也走同一 daemon 保证一致. 实测依据: 独立起进程 28.8ms vs 单进程内 0.2ms, 进程启动占99%  【否决】 (完成 2026-09-10)
  ↳ 断点: 已撤销(方案A被否)。实现并跑通后实测收益仅 27→22ms: node启动20ms逃不掉(CLI必然起进程), daemon只省了读写的5ms。决定不加常驻进程, 改走规则约束(写操作只走MCP, ~2ms)。决策依据已留档概念页 perf-fixed-overhead 避免重提。3个新文件已删, bin/abs.js 已还原, 164测试绿
- [x] ID-SUBSTRING-MATCH — 任务 id 用 includes() 子串匹配定位 → 前缀相同的 id 互相覆盖, 静默丢任务。复现(零并发): 先 task start T11, 再 task start T1 → T11 被 T1 原地改写并消失, 只剩 1 条。三处同源: src/todo.js:219(upsertTask, 会静默改写已有任务+新任务不出现), src/todo.js:250(findTaskLine), src/store.js:325(markDone, 会误标完成别的任务)。改法: 按行首标识符精确比对(取 '- [ ] <id>' 后的 id token 做 ===), 不用 includes。影响: T-1/T-10、ABS-1/ABS-10、fix-hook/fix-hook-2 等任何前缀相同命名  【落地】 (完成 2026-09-10)
  ↳ 断点: 已修(src/todo.js findTaskLine 全等比对 + 新增 idOfTaskLine; store.js markDone 同改)。原复现: 先 T11 再 T1 → 修前只剩1条, 修后两条都在。新增3个回归测试 + revert-check(旧代码3例全失败)。顺带修掉 TASK-SKILL-V2: 的尾部冒号脏 id
- [x] OC-REAL-VERIFY — opencode 真机验证: 通道修完后真会话要看到 teardown-nudge 痕(当前 session.idle:seen 有痕但零 nudge)  【落地】 (完成 2026-09-10)
  ↳ 断点: 依赖已解除: OC-INJECT-CHANNEL 判定=通道本就正常(promptAsync 真能唤醒 idle session), 无需改 src/install.js. 真机已直接看到 teardown-nudge 痕(14:58:36)+注入的 [abs 收尾提醒] 作为 user 消息落地+新 assistant turn 回应. 真机验证的失败条件修正为: 需满足 abs 插件 gate(wroteFiles=true 即本会话真调过 write/edit 工具 + brain 存在 + log.md 今日无记录), 纯文本 turn 不会触发 nudge(设计如此). 可解除 Blocked, 转 Today.
- [x] CC-CODEX-PUSH — CC/Codex 补主动推: Stop hook 回 decision:block 注入收尾指令(守卫抄 pi 插件四条件)，替代只写 wrapup.log  【落地】 (完成 2026-09-10)
  ↳ 断点: 已实现(6b3466f): Stop hook 回 stdout {"decision":"block","reason":<收尾指令>} 把 agent 拉回一轮; 判定收进新命令 abs teardown-check(守卫同 pi 四条件, 异常一律放行 {}); 坏 payload 不阻塞。真机冒烟: Stop 输出合法 JSON + 四守卫逐个拦截验证通过。注意: 全局安装的 abs 是独立副本(1.1.0), 仓库新命令需重新安装才在真会话生效
- [x] OC-INJECT-CHANNEL — opencode 注入通道修复: promptAsync 是 fire-and-forget(204 void)，对终结态 idle session 只入队不重启 turn → 改 src/install.js opencodePluginSource() 用 session.command(同步200) 或把注入点提前到 turn 内(tool.execute.after/message.updated)  【落地】 (完成 2026-09-10)
  ↳ 断点: 判定完成: 结论表=prompt_async/HTTP204/唤醒✓ (2s内新assistant, 真机插件注入 [abs收尾提醒] 作为user消息落地后新turn回应) ; session.prompt/HTTP200/唤醒✓ (同步返回assistant body finish=stop) ; session.command/HTTP200/唤醒✓ ; tui.append-prompt & tui.execute-command/HTTP200 body=true/唤醒✗(无TUI挂载,只入buffer). 真因澄清: 旧测零nudge不是通道故障, 是 wroteFiles gate(纯文本turn不触发write工具); gate满足后真机落痕 teardown-nudge 14:58:36 且注入生效. 建议: 保持 idle 注入点即可, promptAsync 无需改; 若要去掉 fire-and-forget 的不确定性可换 session.prompt(同步可拿到结果/错误). 第四候选(挪到 turn 内 tool.execute.after) 实测也可唤醒(注入while busy会排队为下一turn), 但非必要.
- [x] SMALL-FIXES — 小件: loggedToday 守卫收紧、日志轮转、测试计数 146→实测 144 对平  【落地】 (完成 2026-09-10)
  ↳ 断点: loggedToday 守卫已收紧(10b64de): 裸日期 substring includes() → 正则匹配 log.md 条目头 '## [YYYY-MM-DD HH:MM]'。发现 install.js 双层转义坑(生成代码外层模板 vs 真实 TS), 两处分别验证生成产物正则 source 正确。新回归测试 + revert-check(旧守卫注入0次/新守卫1次), 149 测试全绿。剩: 日志轮转未做(低优先, hooks.log 增长无上限)
- [x] GIT-WRAPUP — git 收尾: 25 文件未提交(今天整个 session log、teardown-automation + host-plugin-silent-failure 概念页、7 个 source 删除)  【落地】 (完成 2026-09-10)
  ↳ 断点: 已提交 2 个 commit: 1f45bf0(代码: abs update/--version + seen 痕 + plugin-behavior.test.js + .gitignore 忽略 .omo/) 和 60bebde(.brain: 2 个新概念页 + 归档 7 源页 + todo 登记)。工作树干净, 测试 148 全绿, lint 0 problem
- [x] VERSION-ALIGN — 版本对齐: 本地 package.json 0.3.0 vs npm 已发布 1.1.0  【落地】 (完成 2026-09-10)
  ↳ 断点: 误判澄清: 本地 package.json 已是 1.1.0 且与 package-lock 一致; npm view @fanchao8609/agent_brain_sync version = 1.1.0 已发布对齐。0.3.0 是旧会话残留读数。npm 直连失败仅因代理 192.168.0.114:7890 未启动, 非版本问题
- [x] ABS-UPDATE-CMD — 补 abs update + abs --version: 用户报'abs update 看不到新版本'实为命令不存在  【落地】 (完成 2026-09-10)
- [x] REALTIME-HOST-VERIFY — 真机验证: pi 会话首次触发 agent_end→seen→nudge 完整链路(11:43:23)  【落地】 (完成 2026-09-10)
- [x] SEEN-TRACE-CWD — seen 痕补记 cwd+brain, 使 nudge 异常可事后解释(曾出现 nudge 但 log.md 有今天, 无法归因)  【落地】 (完成 2026-09-10)
- [x] PLUGIN-SEEN-TRACE — 补 seen 痕可观测性: 首次 agent_end/session.idle 无条件留痕, 否则分不清'事件没触发'与'被守卫拦下'  【落地】 (完成 2026-09-10)
- [x] PLUGIN-BEHAVIOR-TEST — 补插件行为级测试(非字符串断言): 真 import 生成产物 + 驱动真实事件 + 断言日志落痕; 含 ZWSP 守卫  【落地】 (完成 2026-09-10)
- [x] OPENCODE-EXPORT-FIX — op​encode 插件用命名导出(export const)但加载器取 default → 插件被忽略, 改 export default {id, server}  【落地】 (完成 2026-09-10)
- [x] OPENCODE-TEARDOWN — op​encode 补收尾自动化: session.idle + promptAsync 注入(与 pi agent_end 同策略)  【落地】 (完成 2026-09-10)
- [x] OPENCODE-HOOK-DEAD — op​encode 插件 event 回调签名错({name}→{event})+事件名错(session.start→session.created/idle): 插件从未触发过  【落地】 (完成 2026-09-10)
- [x] TEARDOWN-AUTO — 收尾自动化: pi 扩展 agent_end 注入收尾指令(被动记日志→主动推)  【落地】 (完成 2026-09-10)
- [x] CO​DEX-HOOKS-FIX — 修 Co​dex hooks.json 形态错写导致安装崩溃(对象 vs 扁平数组)  【落地】 (完成 2026-09-10)
