---
tags: [session-log, 验证, 测试, 实验台]
id: log-2026-09-17
author: tester
updated: 2026-09-17
status: active
---

# 会话快照 2026-09-17

## 做了什么

### 1. 修了上一条 queryHint 的错误结论（本会话起点）
上会话（及 01:03 那条 log）写着「`cli`(df=13) vs `load`(df=11) 频率不可分 → 调参无解」。
**该结论作废**：实测 `keywords("CLI 结语路径与 help 不一致")` 产出
`['help','store','结语','语路','路径','径与','不一','一致']` —— **`cli` 不在词表里**。
因为 `src/store.js` 的 `queryHint` 先执行 `.replace(/\b[A-Z][A-Z0-9_-]{2,}\b/g,' ')`
把全大写 token 当任务 ID 剥掉。那个 df=13 属于 `query`/`rankPage` 路径，
被错套到了 `queryHint` 上。**论证的对象不存在。**

真实实测三条（见 `sources/2026-09-17-做关键词排序-queryhint.md`）：
1. `CLI` 进不了 hint 词表（大写剥离规则）
2. df 阈值 `T=15/13/12/11` 结果**逐字相同**，在被测区间不起作用
3. 剔掉高频词后 top3 仍是噪音（靠 `结语/不一/一致` 等低频词拼分）

修复方向**未定**，source 页已改写为只含实测事实。

### 2. 给 `abs ab` 补了「判据可比性自检」（**后已随工具删除**）
新增 `checkCriterionComparable(expName, cmd)`，接入 `ab init` 自检链、
`ab check --name`、`ab grade`。查两件事：
- 判据命令在两组跑不起来（`exit 127/126` = **尺子坏了**，不是作品错了）
- 判据在**未改动基线上已全绿** → 两组必同 PASS，差值必然 0

**为何加这道**：本仓原有三道自检查的都是「两组是否干净隔离」（只差 `.brain/`、
目录互不可见、题面逐字相同），但三次实验失败的病根是**判据在两组上不等价** —— 查不到。

### 3. 删除 `abs ab` 与 `abs serve`（本会话最终状态）
用户判定两者都无用：
- **`abs ab`** —— 需人起 agent 才能跑完整实验（工具自己不起 agent），三次尝试均因
  判据不可比失败；**从未成功验证过一轮**。留着是负债。
- **`abs serve`** —— 把 `.brain/` 挂成只读网页（506 行），**无任何测试覆盖**，实际未使用。

删除：`src/ab.js` / `src/serve.js` / `test/ab.test.js`（24 测试）、CLI 两个 case、
import、FLAG_SPEC（`task/name/cmd/force/no-open`）、serve 子用法页、死掉的 `opts.open`、
两条 ab 权限条目。**386 测试全绿（原 410）**、lint 0。

> 跟进的收尾：`resource-default-let-os-assign` 那页的案例来源（`src/serve.js`/`serve()`）
> 已删，已加标注保留规律 + 补了不依赖已删函数的复现手法。
> EXP-BLANK-DIR 四轮实验原始数据随 `/tmp/abs-exp` 删除，仅存 `.brain/` 摘录。
>
> 同被删掉的还有第 2 节之前做的两处修复（arm 后缀碰撞、起点可比性自检）——
> 它们属于已删代码，**教训仍通用**（“用失效坐标删代码”那次失误同样计入下节）。

## 怎么定位的（过程教训，比结论值钱）

**同一病根连犯三次**：2-gram 归因 → df 方向归因 → cli 论证。
全是**没跑就下结论**，且每次都言之凿凿。其中 2-gram 那条连 `keywords("日志")` 都没跑过
（实测 `keywords("日志")` → `["日志"]`，2 字词根本不切）。

**第四次栽在“跑了但测不出东西”**：给后缀碰撞写的测试，断言的是**整个路径**不相等 ——
而路径里本来就有 `-A-`/`-B-` 段，恒真。把哈希回退成 bug 版，测试**照样全绿**。
是**破坏验证抓出来的**；只跑“新测试通过”就会交出空的测试。
→ 已沉淀 `[[vacuous-test-passes-on-broken-code]]`。

**第五次栽在用失效坐标**：删 `serve` 子用法页时用行号 `del lines[269:284]`，
但那是**之前**读的行号，文件已被我自己改过 → 偏移后砍掉 182 行、13 个命令 case
（`todo`/`note`/`install`… 全没）。`npm test` 报 146 fail 才发现。
**根因：用了一个自己已知会失效的坐标**（同为“没验证就下手”族）。
另有一次诊断链里混进 `git stash -q`，静默撤回全部删除。
两次都因**尚未提交**而无不可逆损失。

## 结论
- `abs ab` / `abs serve` **已删除**（见上第 4 节）；本会话前半段为它们做的工作随之作废
- `queryHint` 的旧结论作废；修复方向未定，**别再依据 01:03 那条 log**
- 测试 386 pass / 0 fail；`abs lint` 0 problems

## 关联连接
- [[vacuous-test-passes-on-broken-code]] — 本会话新增：空测试只能靠破坏验证检出
- [[self-authored-evidence]] — 本会话三次"没跑就下结论"同源
- [[symbol-reference-needs-real-run]] — 同样主张"断言存在 ≠ 被验证过"
- [[host-plugin-silent-failure]] — 删除后遗留的悬空引用同属“零信号”族
- [[2026-09-17-做关键词排序-queryhint]] — 本次更正的那条 source
- [[resource-default-let-os-assign]] — 案例来源（`serve`）本次被删，已加标注

## 🪝 Next Session Hook
**入手上哪找**：
1. `abs load` → Rules 15 条 + 无滞留
2. **要动 `queryHint`/关键词排序前先读** `sources/2026-09-17-做关键词排序-queryhint.md`
   —— 里面三条实测否证，避免重试；**别信 log 里 01:03 那条**（已作废，见 01:19 更正）
3. `abs ab` / `abs serve` **已不存在**（本会话删除），别再试图调用

**未决**：
- **hint「主题词」判据未找到** —— 缺尺子时别动手（本会话三次翻车都在缺尺子时发生）。
  要做得先搭台（判据：hint 命中 vs 人工标注）
- `queryHint` 修复方向**未定**；已排除的两条（df 阈值、命中位置）在 source 里
- **参照系已失效**：`abs ab` 删除后，本仓**没有任何验证 abs 有效性的手段**了
  （读 308 : 检索 3 的读写比仍是未解决的问题）
- EXP-BLANK-DIR 四轮实验的**原始数据已随台子删除**，结论仅存 `.brain/` 摘录
