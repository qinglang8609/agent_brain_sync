---
tags: [source, concurrency, audit]
updated: 2026-09-08
status: draft
---

# 来源：并发写安全审计心法: 别对'所有写入'一律加锁, 先按风险分类——①读改写(读旧

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- 并发写安全审计心法: 别对'所有写入'一律加锁, 先按风险分类——①读改写(读旧覆盖别人新行)才需锁; ②append日志短行O_APPEND单write天然原子不需锁(加锁反拖慢fire-forget热路径); ③新文件tmp+rename原子写天然安全; ④单用户离散动作(install)不必锁; ⑤门控(requireBrain). 判断基准是'会不会丢更新'而非'有没有写'.

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
