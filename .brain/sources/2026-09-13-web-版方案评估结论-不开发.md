---
tags: [source, 决策, web, 需求评估]
author: fanchao
updated: 2026-09-13
status: draft
---

# 来源：web 版方案评估结论：不开发。理由=价值不足——abs 的核心是 hook 自动触发+CLI 快，浏览器手动打开一个只读/编辑器页面，比直接编辑 .md…

TITLE: web 版方案评估结论：不开发。理由=价值不足——abs 的核心是 hook 自动触发+CLI 快，浏览器手动打开一个只读/编辑器页面，比直接编辑 .md 文件或让 AI 改没有任何增量。且读写 web 端会引入服务端并发/鉴权/路径安全三重复杂度，与「零运行时依赖 + .brain 就地存 markdown」的约束冲突。若要可视化，现成工具(Obsidian/VS Code)已覆盖，不必自造。定位判断：这是需求幻觉而非真痛点。

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- web 版方案评估结论：不开发。理由=价值不足——abs 的核心是 hook 自动触发+CLI 快，浏览器手动打开一个只读/编辑器页面，比直接编辑 .md 文件或让 AI 改没有任何增量。且读写 web 端会引入服务端并发/鉴权/路径安全三重复杂度，与「零运行时依赖 + .brain 就地存 markdown」的约束冲突。若要可视化，现成工具(Obsidian/VS Code)已覆盖，不必自造。定位判断：这是需求幻觉而非真痛点。

## 关联连接
- [[fanchao]] — 本页沉淀者
（提炼成 concepts 规律页后，在此挂双链到该页）
