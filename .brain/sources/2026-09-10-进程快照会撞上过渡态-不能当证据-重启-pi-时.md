---
tags: [source, 坑, 判据, 进程, 过渡态]
updated: 2026-09-10
status: draft
---

# 来源：进程快照会撞上过渡态，不能当证据：重启 pi 时我抓到一张 ps，看到两个 mc

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- 进程快照会撞上过渡态，不能当证据：重启 pi 时我抓到一张 ps，看到两个 mcp.js 就断定"孤儿进程泄漏"。实际旧进程正处于退出中——关 stdin 的决定性实验证明 bin/mcp.js 会自退(code=0)。教训：判"泄漏/卡死"必须做确定性实验（关 stdin / 发信号，看它是否在预期内退出），而不是看一次进程列表。这和本次会话另外几次同源（readTodo/锁/脏数据三次误判）：单次观察≠证据，要先给一个能区分两种假设的实验。

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
