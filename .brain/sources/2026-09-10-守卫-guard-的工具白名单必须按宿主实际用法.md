---
tags: [source, true]
updated: 2026-09-10
status: draft
---

# 来源：守卫(guard)的工具白名单必须按宿主实际用法穷举, 漏一个就把功能静默关掉。

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- 守卫(guard)的工具白名单必须按宿主实际用法穷举, 漏一个就把功能静默关掉。opencode 侧 wroteFiles 守卫抄 pi 时漏了 bash, 而该宿主大量修改走 bash(heredoc/sed), 纯 bash 会话永不置位 → 提醒静默不发, 却被误诊为"注入通道坏了"整整一轮 守卫,白名单,静默失效,误诊

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
