---
tags: [source, npm, git, proxy, 发布]
author: fanchao
updated: 2026-09-13
status: draft
---

# 来源：git 配了失效代理(192.168.0.114:7890) → push 报 Failed to connect port 7890，但直连本就通。绕过:…

TITLE: git 配了失效代理(192.168.0.114:7890) → push 报 Failed to connect port 7890，但直连本就通。绕过: git -c http.proxy= -c https.proxy= push。同 npm 那条教训，已并入 npm-publish-flow 坑。

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- git 配了失效代理(192.168.0.114:7890) → push 报 Failed to connect port 7890，但直连本就通。绕过: git -c http.proxy= -c https.proxy= push。同 npm 那条教训，已并入 npm-publish-flow 坑。

## 关联连接
- [[fanchao]] — 本页沉淀者
（提炼成 concepts 规律页后，在此挂双链到该页）
