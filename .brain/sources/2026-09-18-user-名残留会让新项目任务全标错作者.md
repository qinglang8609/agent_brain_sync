---
tags: [source, 坑, 配置]
id: 2026-09-18-user-名残留会让新项目任务全标错作者
author: fanchao
updated: 2026-09-18
status: draft
---

# 来源：user 名残留会让新项目任务全标错作者：~/.abs/config.json 的 {user} 是单一全局值，一次手动 'abs config set…

TITLE: user 名残留会让新项目任务全标错作者：~/.abs/config.json 的 {user} 是单一全局值，一次手动 'abs config set user tester' 会永久污染之后所有项目（实测新项目看板每条都标 [[tester]]）。诊断三步：cat ~/.abs/config.json → 有值就是它，无值才去查 ABS_USER 环境变量。测试隔离本身是好的（ABS_CONFIG_DIR 指向沙箱），所以见到陌生用户名先怀疑残留、别先怀疑代码。

WHEN: 新项目里任务/日志的作者标记是陌生人名（tester/foo）时

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- user 名残留会让新项目任务全标错作者：~/.abs/config.json 的 {user} 是单一全局值，一次手动 'abs config set user tester' 会永久污染之后所有项目（实测新项目看板每条都标 [[tester]]）。诊断三步：cat ~/.abs/config.json → 有值就是它，无值才去查 ABS_USER 环境变量。测试隔离本身是好的（ABS_CONFIG_DIR 指向沙箱），所以见到陌生用户名先怀疑残留、别先怀疑代码。
- 何时读：新项目里任务/日志的作者标记是陌生人名（tester/foo）时

## 关联连接
- [[fanchao]] — 本页沉淀者
（提炼成 concepts 规律页后，在此挂双链到该页）
