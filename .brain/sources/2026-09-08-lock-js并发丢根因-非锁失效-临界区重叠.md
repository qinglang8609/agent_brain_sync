---
tags: [source, true]
updated: 2026-09-08
status: draft
---

# 来源：lock.js并发丢根因: 非锁失效(临界区重叠=0完美互斥), 而是 LOCK

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- lock.js并发丢根因: 非锁失效(临界区重叠=0完美互斥), 而是 LOCK_MAX_WAIT_MS=3s太短, 高并发15-30进程抢一锁时后到进程排队累积超3s抛LockTimeout崩溃丢写入。修: 预算3s→30s + 重试300ms固定→指数退避15ms起; 合成editFile N=30全过, 但真实15 CLI同刻task start仍偶发丢(时序极敏感, 实际使用N≤8不丢) 坑,lock,并发

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
