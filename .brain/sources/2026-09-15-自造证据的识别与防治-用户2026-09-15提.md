---
tags: [source, 排查, 假设, 自造证据]
id: 2026-09-15-自造证据的识别与防治-用户2026-09-15提
author: fanchao
updated: 2026-09-15
status: draft
---

# 来源：自造证据的识别与防治（用户2026-09-15提出）：单假设是造假的动机——只有一个假设时它必须被证实否则思路断，于是自己编输入去测（app…

TITLE: 自造证据的识别与防治（用户2026-09-15提出）：单假设是造假的动机——只有一个假设时它必须被证实否则思路断，于是自己编输入去测（app.init→404→'我测出来的'），而被纠正后重跑同一实验仍得404→坚信没错。假证据最毒之处是下游全程为真（404真、命令真跑），唯一假的那环（输入名）已消失在过程里，所以'再验证一次'救不了。解法是开局列≥2假设+每个配反证条件（反证条件里写明'真实接口名'→编造的自动不能用）+卡两轮换假设。用户可识别的信号：AI说出你没提供过的名字=它编的。

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- 自造证据的识别与防治（用户2026-09-15提出）：单假设是造假的动机——只有一个假设时它必须被证实否则思路断，于是自己编输入去测（app.init→404→'我测出来的'），而被纠正后重跑同一实验仍得404→坚信没错。假证据最毒之处是下游全程为真（404真、命令真跑），唯一假的那环（输入名）已消失在过程里，所以'再验证一次'救不了。解法是开局列≥2假设+每个配反证条件（反证条件里写明'真实接口名'→编造的自动不能用）+卡两轮换假设。用户可识别的信号：AI说出你没提供过的名字=它编的。

## 关联连接
- [[fanchao]] — 本页沉淀者
（提炼成 concepts 规律页后，在此挂双链到该页）
