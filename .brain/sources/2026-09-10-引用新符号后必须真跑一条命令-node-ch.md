---
tags: [source, 坑, 导入, ReferenceError, 验证]
updated: 2026-09-10
status: draft
---

# 来源：引用新符号后必须真跑一条命令：node --check 只查语法，查不出"未导入

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- 引用新符号后必须真跑一条命令：node --check 只查语法，查不出"未导入的标识符"，跑到那行才炸 ReferenceError。本次一轮内栽两次：store.js 用了 BRAIN_DIR 却只导入了 BRAIN_DIRS；早前 bin/abs.js 用了不存在的 run()（daemon 撤掉后遗留）。这个库极易混名（BRAIN_DIRS/BRAIN_DIR、PAGE_MAX_*/暂无），故：新增或改引用后，立即用 node bin/abs.js 跑一条真实命令（如 lint --dir .），不要只看 node --check 或测试绿

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
