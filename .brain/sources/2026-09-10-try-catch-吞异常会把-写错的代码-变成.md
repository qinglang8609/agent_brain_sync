---
tags: [source, 坑, 静默失效, 异常吞没, 路径]
updated: 2026-09-10
status: draft
---

# 来源：try/catch 吞异常会把「写错的代码」变成「永远不触发的检查」——本次 l

## 记录（实时暂存，Teardown 时提炼进 concepts/ 后本页可删）
- try/catch 吞异常会把「写错的代码」变成「永远不触发的检查」——本次 lint 的 DONE-PILED-UP 永不报警, 根因是 brainPath(vault,'todo.md')(vault 已是 .brain 目录 → 多拼一层 .brain/.brain/todo.md → ENOENT) 被外层 try/catch 吃掉。教训: ①容错只对预期的错误码做缺省(.catch(()=>'') 处理 ENOENT), 不要用 try/catch 包整段逻辑 —— 后者会连程序错一起吞 ②新增的"只读检查"必须验证它真能报出来, 否则等于没写

## 关联连接
（提炼成 concepts 规律页后，在此挂双链到该页）
