---
name: abs-agent-brain-sync
description: >-
  abs (agent-brain-sync) 跨会话 AI 编码记忆与任务续接。用户说/敲 `abs`（abs save 存档、
  abs load 开场、abs query 查询、abs lint 体检、或意图不明时）即触发：先在项目根找
  `.brain/` 图谱，无则建档；再按用户意图分流——存经验(Teardown)、查知识(query)、
  或默认读 todo 看板续做未完成任务(Init)。解决 Claude/OpenCode/Cursor 等会话无状态导致
  经验碎片化、每轮重开重新摸索的问题。图谱为 Obsidian 可读的 [[双链]] Markdown，
  零依赖。想存经验/查以前踩坑/续上次任务/开始用记忆功能时用。
---

# 🧠 abs-agent-brain-sync

把 AI 编码经验从会话沙盒里救出来。每个会话都是无状态的——Claude、OpenCode、Cursor
各开一堆会话，经验/进度/踩坑全碎片化，重开像失忆。本技能用一个放**项目根目录**、
Obsidian 可直接打开的 Markdown 图谱（`.brain/`）做统一落点。
零依赖、不碰原始代码之外的文件；模板全内联，只读安装也能手工建档。

## 触发总入口（每次命中技能，第一步先走这里）

技能被触发（用户问话、开新会话、或说 `abs ...`）时，**第一步永远是下面这条链**，
然后再看用户真正要什么：

1. **找图谱**：在当前项目根搜 `.brain/`。
2. **没有？先建档**：若项目还没有 `.brain/`，先建新档案——按[初始化](#初始化给一个项目装图谱)建空图谱
   （有 `bootstrap.sh` 就跑它；没有就照「文件标准」手工建骨架），然后回到第 3 步。别在没图谱时就开写。
3. **有？分析用户意图**，分流到对应流程：

| 用户意图（说/敲的） | 判断 | 走哪 |
|--------------------|------|------|
| 总结经验 / 结束了 / `abs save` / “把这次记下来” | 用户要**沉淀**这次会话的坑/决策/进度 | [收尾：Teardown Sync](#收尾teardown-sync) |
| 查询 / 问坑 / `abs query <词>` / “我上次怎么解决 X 的” | 用户要**检索**已有知识 | [维护 query](#维护ingest--query--lint) |
| 检查图谱健康 / `abs lint` | 用户要**体检** | 维护 lint（跑 `scripts/lint.sh`） |
| 开新任务 / 继续开发 / “帮我做 X” | 用户要**开工**（开发中） | [开场：Init Sync](#开场init-sync)，之后正常做任务 |
| 无法归类 / 其他闲聊 / 意图不明 | **默认续 todo**：读 `todo.md` 看 `[In Progress]`/`[Todo]` 未完成项 → 有就[开工续做](#开场init-sync)；没有再看用户真正想干什么 | [开场：Init Sync](#开场init-sync) |

**判断原则**：意图从用户当前这句话里分析，别猜。意图不明时默认**续 todo**——这是本技能的主心骨：
任何情况下先接上未完成的工作，而不是停在闲聊。
**进行中就维护 todo**：凡是本技能命中后的开发会话，`todo.md` 要当**活看板**随手更新——
子任务做完/碰壁/被改需求就立刻落 todo，别等收尾。详见[进行中纪律](#进行中todo-是活看板最重要的纪律)。
**收尾自动沉淀**：当会话要结束（用户说“结束/先这样/切到别的事/改天再说”，或长任务告一段落）时，
**主动提议沉淀**——用一句话确认：`要我把这轮的坑/进度记进 .brain 吗？` 用户同意或默认继续，就走
[收尾：Teardown Sync](#收尾teardown-sync)。这是救回“忘了保存就丢经验”的关键，但只**提议不静默写**，
避免强制沉淀出噪音。

## 图谱长什么样（都在 `.brain/` 下）

```
.brain/
├── index.md        # 总索引 + 当前路线(Roadmap)。所有知识页一行索引，入口。
├── log.md          # 操作流水：每次 ingest 追加一行摘要，倒序。精简，不写正文。
├── todo.md         # 动态看板：进行中/待办/阻塞/已完成。进度唯一真源。
├── entities/       # 实体页：一个"具名事物"一页。工具/库/服务/API/模块/人物。
├── concepts/       # 概念页：一个"可复用规律"一页。坑/模式/方法论/术语/架构决策。
├── sources/        # 暂存页：一次会话的一手记录。临时，提炼完即归档/删。见尺寸纪律。
├── syntheses/      # 综合页：跨实体、跨会话的横向判断/选型综述/路线。
└── sessions/       # 会话快照 + Next Session Hook：给下个会话的接力棒。
```

> 目录名 `.brain/` 简短即可；`.brain/` 前导点让 git/文件树默认隐藏，降低误动概率。

### 归类规则（新知识进哪类）

| 类别 | 放什么 | 命名 |
|------|--------|------|
| `entities/` | 具名的**事物**：`docker`、`auth-module`、某 API | TitleCase：`Docker.md` |
| `concepts/` | 可复用的**规律/坑**：`docker-prisma-429`、迁移清单 | kebab-case：`docker-prisma-429.md` |
| `sources/` | 某次会话的**一手记录**（事实层，临时） | `YYYY-MM-DD-slug.md` |
| `syntheses/` | **横向综合**：选型对比、架构取舍 | `synthesis-slug.md` |

判断一问：这是"一样东西"还是"一条规律"？
- 能说"它是什么"→ `entities/`；能说"这么做就避坑"→ `concepts/`。
- 卡住默认 `concepts/`——规律比实体更易被下个会话命中。

## 容量纪律（最重要的节——别什么都往里扔）

图谱贵在**精**不在全。每写一页前先过下面四关，不过就不写或压缩：

1. **再命中测试**：下个会话若不知道这条，会不会踩同一个坑 / 重做同一个决定？
   会 → 存；不会 → 不存。普通实现细节、能靠 `grep` 从代码读出的东西，一律不记。
2. **单文件硬上限**：`entities/ concepts/ syntheses/` 单页正文建议 **< 150 行 / < 5KB**。
   超了：拆成子概念，或只留结论 + `## 见 [[相关页]]` 外链。别把一页写成搬运工。
3. **sources 是暂存区，不是存档柜**：`sources/YYYY-MM-DD-slug.md` 只留"这次会话里值得提炼的原始线索"，**提炼完的规律迁进 concepts/entities 后，source 页即可归档或删**（删除时同步清掉指向它的引用，见 Teardown 第 6 步）。别让 sources 无限堆积——旧会话的价值在规律里，不在流水账里。
4. **写前压缩三问**：①这是规律还是噪音？②不记会怎样？③能不能**用一行链接到已有页**代替新增整页？（能 → 改链，不建新页。）

`lint.sh` 会检查单页超限与 sources 堆积，见[维护](#维护ingest--query--lint)。

## 初始化：给一个项目装图谱

首次用（项目还没有 `.brain/`）时：

1. 建空图谱：`./scripts/bootstrap.sh <项目根>`（若无可执行权限先 `chmod +x scripts/bootstrap.sh`）。
   若脚本不可用（沙盒/只读安装），按下节「文件标准」手工建 3 个骨架文件 + 6 个空目录即可——
   模板已内联，无需别的子文件。建好即日常可用。
2. 每类文件的模板见下节「文件标准：图谱内每个文件的样子」，开新页照着套格式（不再依赖
   本技能子文件夹）。
3. 把本技能接到目标项目：在项目根放一行指向本技能的引用，或让该项目的 AI 助手
   按 description 命中本技能（见末节"接入"）。

## 开场：Init Sync（开工 / 默认续 todo）

这是[触发总入口](#触发总入口每次命中技能第一步先走这里)里“开工”与“默认续 todo”意图走的流程。
图谱已存在；收到用户第一个核心开发指令**之前**，先走这条链载入上下文，做完才动工：

1. 读 `index.md` 定当前路线与优先级；读 `todo.md` 看进行中的事，别盲目开辟无关改动。
2. 按用户输入关键词，在 `index.md` 定位命中的页 → 进对应 `entities/ concepts/` 目录读全文。
3. **续 todo**：若本次是“默认续 todo”分支（无明确新任务），把 `todo.md` 顶部未完成项当作
   当前要做的事直接开做；没有未完成项则问用户想做什么。
4. **登记新任务**：若用户给了**明确新任务**而 `todo.md` 里没有，**开工前先把任务写进
   `todo.md`**（`[Todo]` 或 `[In Progress]`）再动工。别让任务只活在当前会话——todo 是跨会话
   唯一真源，不先登记，会话一切断就丢。

**完成标准**：能答“现在该做什么（含未完成 todo）、以前踩过什么相关坑”；关键页已进上下文；
并在无新指令时已接上最高优先未完成项。不是背书式念目录。

## 进行中：todo 是活看板（最重要的纪律）

**todo.md 不是收尾仪式，是干活中就随改随写的活看板。** 旧版把 todo 更新框死在“收尾
Teardown”第 4 步——那是会话结束时才跑的一次，结果智能体开场读一次后闷头干很久、只在
收尾写一次：会话一被截断/打断/context 满了，todo 就没落盘，下个会话接不上。打破它：

**每个任务边界都立即更新 `todo.md`，别拖到收尾。** 触发时机（三选一就更新，和 git commit
同一个反射）：

- **一个子任务做完了** → 立即把它从 `[Todo]`/`[In Progress]` 移 `[Done]`（附日期）；若有后续，
  顺手把下一步写进 `[Todo]`。
- **碰壁/阻塞了** → 立即移 `[Blocked]`，附最近一次失败输出 + 卡点一句话。
- **被用户打断/改需求/重定向** → 立即把已做与未做的边界写清楚：做完的移 `[Done]`，
  半成品与没动的放回 `[Todo]`（半成品注明“做到哪一行/哪个文件”，方便下会话续）。

更新只动 `todo.md` 对应行，快、准、不啰嗦；不为此开 sources/concepts 页。
任务全部做完时，顺手把 `[In Progress]` 置空。

**判断**：只要当前会话里任务状态变了（推进/卡住/改向），就该此刻落 todo，而不是等收尾。
只有“任务毫无进展、状态没变”才不需要更新。

## 收尾：Teardown Sync

任务告一段落 / 用户要切会话 / 结束前，把本次**真实发生**的写回图谱。只写发生过、跑过、
测过的事实，禁止脑补未实现架构。按序执行，每步都有落点：

1. **记一手线索** → 建 `sources/YYYY-MM-DD-slug.md`：浓缩做了什么、改了哪些文件、验证命令
   与结果。链到你命中的 `entities/ concepts/`。
2. **抽规律（凡容量纪律第 1 关通过）** → 写/更新 `concepts/<kebab-slug>.md`：
   `触发场景 / ❌表现(贴报错) / 🛠根因+修复+验证命令`。挂到相关 `entities/<X>.md` 名下双链。
3. **沉淀实体** → 若碰了项目重要且未记录的"具名事物"，建/更新 `entities/<X>.md`：它是什么、
   在这项目里怎么用、坑指向哪。
4. **对账进度** → `todo.md`：干活中应按[进行中纪律](#进行中todo-是活看板最重要的纪律)随改随写了，
   这里只做最终对账——把收尾时仍滞留 `[In Progress]` 的归位（真做完移 `[Done]`，半成品回
   `[Todo]` 注明断点）；遗留/新发现 bug 写明放 `[Todo]` 或 `[Blocked]`（Blocked 附最近一次失败
   输出）。没中途落盘的此刻补上。
5. **综合(可选)** → 本次推进了跨模块选型/取舍/路线 → 更新 `syntheses/`。
6. **收拢 sources** → sources 里已提炼成规律的，删或归档（容量纪律第 3 关）。**删某 source 时，
   必须同步清掉所有指向它的 `[[...]]`（`index.md` 条目、session 页里的引用）**——否则 lint 报死链，
   接力棒断裂。
7. **修 index + 记 log** → 每个新建/大改页同步进 `index.md`；`log.md` 倒序追加**一行**摘要。
8. **留接力棒** → `sessions/log-YYYY-MM-DD.md`：记日期/分支/主要变更，**强制**写
   `## 🪝 Next Session Hook`：下个会话该从**哪个文件哪一行/哪个函数**继续、入参参考哪页。

**完成标准**：每条过了容量纪律的知识在图谱有**一处**落点；`index.md` 与事实一致；
`sessions/` 多一份带 Hook 的快照。同一事实别写进多页（每页单一事实源）。

## 页面格式（所有知识页共用）

开头用 YAML frontmatter：

```markdown
---
tags: [concept, 领域]        # entity|concept|source|synthesis|session-log
updated: YYYY-MM-DD
status: draft                # 或 reviewed（人工/被下会话验证过）
sources: []                  # 引用来源，页名数组
size_ok: true                # 正文 <150行/<5KB 时填 true；lint 检查用
---
```

正文强制三点：
- **关联连接区**：每页必须有 `## 关联连接`，用 `[[页面名]]` 链到相关页。**严禁孤岛页**。
- **知识冲突**：新知识若与旧页冲突，不静默覆盖。加 `## 知识冲突` 区块，两版都留、标来源与
  时间，交人工裁决（裁决后把 `status` 改 `reviewed` 表示结案）。
- **命名即链接**：Obsidian 双链靠文件名。`[[Docker]]` 落 `entities/Docker.md`；
  `[[docker-prisma-429]]` 落 `concepts/docker-prisma-429.md`。别建"链接名≠文件名"的别名层。

## 文件标准：图谱内每个文件的样子（照着抄即可，含空文件）

下面给 `.brain/` 里每个文件的完整骨架。**任何 AI 不看别的子文件也能靠本节建出完整图谱**。
新建某类页时复制对应模板，填实内容、删占位示例。写好的页要过容量纪律与 lint。

### 1) 骨架文件：`index.md` / `log.md` / `todo.md`（首次建图时写入）

`index.md` —— 总入口 + 路线，每新建/大改一页必须同步加一行 `[[页名]] — 一句话`：

    # 🗂 图谱索引

    本文件唯一入口。每新建/大改一个知识页，同步在此分类下加一行 `[[页面名]] — 一句话`。

    ## 当前路线 (Roadmap)
    > 里程碑与方向（可链到 syntheses/）。

    ## Todo
    - [[todo]] — 进行中/待办/阻塞/已完成（状态唯一真源）

    ## Concepts
    ## Entities
    ## Sources
    ## Syntheses
    ## Sessions

`log.md` —— 操作流水，倒序（新在上），只记一行摘要不写正文：

    # 🗒 操作日志
    YYYY-MM-DD | ingest | 沉淀 docker-prisma-429

`todo.md` —— 进度唯一真源：

    # 📋 Todo 看板
    ## In Progress
    - [ ] 最高优先级任务放最上
    ## Todo
    - [ ] 任务
    ## Blocked
    - [ ] 任务 — 原因 + 最近失败输出
    ## Done
    - [x] 任务 — 完成日期

### 2) 概念页 `concepts/<kebab-slug>.md`（规律/坑/方法论，容量硬上限 150 行/5KB）

    ---
    tags: [concept, 领域]
    updated: YYYY-MM-DD
    status: draft            # 或 reviewed
    sources: []
    size_ok: true
    ---

    # 概念：一句话（文件名即双链 [[kebab-slug]]）

    ## 触发场景
    在做什么、用什么框架时遇到此坑/用到此规律。

    ## ❌ 表现
    报错/异常（贴真实输出）：
    （此处贴核心报错，代码块或缩进行）

    ## 🛠 解法
    1. 根本原因：一句话。
    2. 关键代码/配置：
       （此处放修复片段，缩进 4 空格）
    3. 验证命令：跑什么证明已好。

    ## 关联连接
    - [[EntityName]] — 挂在这个工具/模块下
    - [[another-concept]]

    ## 知识冲突
    （与旧页矛盾才写：两版都留、标来源与时间。无则不写。）

### 3) 实体页 `entities/<TitleCase>.md`（具名事物，硬上限同概念页）

    ---
    tags: [entity, 领域]
    updated: YYYY-MM-DD
    status: draft
    sources: []
    size_ok: true
    ---

    # 实体名（文件名即双链 [[EntityName]]）

    ## 是什么
    一句话定义 + 在本项目的角色。

    ## 本项目里怎么用
    - 引入/安装
    - 关键用法
    - 版本/约束

    ## 相关概念（名下踩过的坑）
    - [[docker-prisma-429]]

    ## 关联连接
    - [[synthesis-slug]]

### 4) 来源页 `sources/YYYY-MM-DD-slug.md`（暂存区，提炼完即归档/删）

    ---
    tags: [source]
    updated: YYYY-MM-DD
    status: draft
    ---

    # 来源：本次会话浓缩

    ## 做了什么
    - [x] 完成 X（改哪几处文件）
    - [ ] 遗留 Y

    ## 关键决策
    为什么这么选（一句话）。

    ## 验证
    跑了什么命令、结果。

    ## 抽出的规律
    - → [[concept-slug]]（本页提炼完迁入 concepts/，本页即可删）

    ## 关联连接
    - [[Docker]]

### 5) 综合页 `syntheses/<synthesis-slug>.md`（跨会话横向判断，硬上限同概念页）

    ---
    tags: [synthesis]
    updated: YYYY-MM-DD
    status: draft
    sources: []
    size_ok: true
    ---

    # 综合：一句话结论

    ## 权衡
    | 方案 | 优点 | 缺点 | 适用 |

    ## 决策依据
    为什么选某路（链到相关 sources/concepts）。

    ## 关联连接
    - [[EntityName]]
    - [[YYYY-MM-DD-slug]]

### 6) 会话页 `sessions/log-YYYY-MM-DD.md`（快照 + 接力棒）

    ---
    tags: [session-log]
    updated: YYYY-MM-DD
    status: draft
    ---

    # 会话 YYYY-MM-DD

    ## 变更
    - [x] X；[ ] 遗留 Y

    ## 🪝 Next Session Hook（强制）
    下会话从哪个文件哪一行/哪个函数继续、入参参考哪页。

    ## 关联连接
    - [[YYYY-MM-DD-slug]]

## 维护：ingest / query / lint

- **ingest（沉淀）**：把一次产出浓缩进 `sources/`、抽规律进 `concepts/`、更新
  `index.md`+`log.md`、收拢 source。见 Teardown 第 1–7 步。
- **query（检索）**：先读 `index.md` 定位 → 读命中页 → 回答用 `[[页面名]]` 标来源。跨多跳
  问题沿双链走，不只字面匹配。**问“代码里某符号在哪/谁调用/怎么改会波及谁”这类当下代码问题，
  答案不在 `.brain` 记忆里，直接读源码定位**；`.brain` 只答“以前踩过什么坑、上次做到哪”。
- **lint（体检）**：跑 `./scripts/lint.sh <图谱根>`，自动查：死链、孤岛页、缺 frontmatter、
  模板残留、未决冲突、单页超尺寸、sources 堆积、**index 漏列新页**。按报告修复
  （死链→补页/改链；孤岛→补 `## 关联连接`；冲突→裁决改 `reviewed`；超大页→拆或外链；
  sources 积压→提炼归档；INDEX-MISSING→把页名补进 `index.md`）。

## 接入（把技能接到目标项目）

本项目是技能本体。要用到某个编码项目时：
- 在项目 `.pi/skills/`（pi）或 `.claude/skills/` / `.opencode/`（其他）放本技能一份，
  或在该项目的 `AGENTS.md` / `CLAUDE.md` 顶部加一行：
  > 命中 `abs-agent-brain-sync` 时，先走其「触发总入口」：找 `.brain/`（无则建档），
  >  再按用户意图分流（沉淀/查询/开工）。
- 用户敲 `abs load/save/query/lint` 只是显式意图词，同样由[触发总入口](#触发总入口每次命中技能第一步先走这里)分流，无需额外装文件。

## 自我约束

- 只读写 `.brain/` 与目标代码，不动用户本地全局配置。
- 内容基于当前会话真实发生的事实；遵守容量纪律，宁缺毋滥。
- 双链/frontmatter/`index.md` 必须自洽——图谱是给下个会话读的，坏链=掰断接力棒。
